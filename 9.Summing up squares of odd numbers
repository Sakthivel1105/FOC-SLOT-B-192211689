//9.Summing up squares of odd numbers
#include<stdio.h>
int main()
{
    int a=1,sum=0,n;
    printf("\n Enter a number:");
    scanf("%d",&n);
    while (a<=n)
    {
       sum=sum+(a*a);
       a=a+2;
    }
    printf("\n sum= %d",sum);
    return 0;
}
